# Копирай като .env.docker и промени при нужда
# cp .env.docker.example .env.docker

# Публичен URL (за VM в облак: http://ТВОЯТ_IP)
PUBLIC_URL=http://localhost

# JWT – смени в production
JWT_SECRET=c3ByaW5nLWNhcnBvb2wtand0LXNlY3JldC1rZXktMTIzNDU2
JWT_EXPIRY_HOURS=24

# MySQL
MYSQL_ROOT_PASSWORD=root

# CORS (автоматично от PUBLIC_URL ако е празно)
# CORS_ORIGINS=http://1.2.3.4:[*]

# Mail (по избор)
APP_MAIL_ENABLED=false

# Stripe (по избор)
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
