# ГЛАВА IV — Реализация (подобрен академичен текст)

**За Word:** Копирайте от „ГЛАВА IV…“ до „ЗАКЛЮЧЕНИЕ“. На **[Фиг. 4.X]** — скрийншот (`localhost:5173`). Кодът под всяка фигура е **вграден в текста** — копирайте го в Word с шрифт Courier New 9–10 pt като „Фиг. 4.X“ (код).

---

# ГЛАВА IV. РЕАЛИЗАЦИЯ И ОСНОВНИ ФУНКЦИОНАЛНОСТИ НА РАЗРАБОТЕНОТО УЕБ ПРИЛОЖЕНИЕ

В настоящата глава се разглежда практическата реализация на уеб приложението за организиране на споделени пътувания. Ако в глава II са формулирани изискванията към системата, а в глава III — нейното проектиране (технологии, архитектура, потребителски интерфейс и модел на данните), то тук акцентът пада върху това **как тези решения са въплътени в работещ софтуер** и как изглежда приложението от гледна точка на крайния потребител.

Разработената система позволява на шофьори и пътници да се откриват взаимно, да публикуват и търсят пътувания между градове, да резервират места, да уточняват точки за качване и слизане по маршрута, да комуникират чрез вграден чат и при необходимост да извършват плащане. Реализацията следва класическия **клиент–сървърен** модел: в браузъра се изпълнява single-page application (SPA) на **React** и **TypeScript**, а сървърът предоставя **REST API** на **Spring Boot**, като данните се съхраняват в **MySQL** и се управляват чрез миграции **Flyway**.

Съществената разлика спрямо по-старите уеб приложения със сървърно генерирани страници (например с Thymeleaf) е, че тук **целият потребителски интерфейс се рендира в браузъра**, а сървърът връща само структурирани данни във формат JSON. Удостоверяването на потребителя се извършва чрез **JSON Web Token (JWT)** — при всяка защитена заявка клиентът изпраща заглавка `Authorization: Bearer <token>`, без да се поддържа сървърна HTTP сесия (политика **stateless** в Spring Security). Този подход е широко разпространен в съвременните уеб системи и улеснява разделянето на frontend и backend, тестването на API (Swagger) и бъдещото разширяване на приложението.

Структурата на главата следва логиката, приета в образец от друга дипломна работа: първо се обобщават **основните функционалности** и **потребителските роли** (раздел 4.1), след което следва **програмната реализация** с описание на екрани, бизнес логика и илюстративни фрагменти от изходния код (раздел 4.2).

---

## Основни функционалности

След успешна регистрация и вход в системата всеки потребител получава достъп до ядрото на приложението — преглед и търсене на обявени пътувания. Той може да филтрира резултатите по град на тръгване и пристигане, по дата, както и да подреди обявите по време на тръгване или по цена. При избор на конкретно пътуване се визуализират неговите детайли: час на тръгване, цена, брой свободни места, информация за шофьора и **интерактивна карта** с маршрута и спирките по него.

Пътникът може да **резервира място**, да посочи точки за качване и слизане в рамките на допустимия коридор около маршрута, да добави бележка към шофьора и да проследи състоянието на заявката си в раздел „Мои резервации“. След одобрение от шофьора плащането може да се извърши **в брой** при пътуването или **с банкова карта** чрез интеграция със Stripe. За координация между страните е предвиден **чат**, свързан с конкретно пътуване, както и **известия** в приложението (нова заявка, одобрение, отказ, необходимост от плащане). След приключено пътуване участниците могат да оставят **взаимни оценки**, които се отразяват в профила. Потребителят поддържа личните си данни в **профила** си, може да смени паролата си или да използва механизъм за **възстановяване на забравена парола**.

Потребител, който **обявява пътувания** (в терминологията на системата — шофьор), разполага с допълнителни възможности: създаване на нова обява, преглед на постъпилите резервации, **одобрение или отказ** на заявки, преглед на **маршрута при тръгване** с подредени точки за забиране на пътници, маркиране на получено плащане в брой и приключване на пътуването. При нужда шофьорът може да сподели своето местоположение с пътник по GPS.

**Администраторът** не участва в типичния поток „шофьор – пътник“, а поддържа платформата: преглед на регистрираните потребители, блокиране и отблокиране на акаунти, както и преглед на обобщена статистика за системата.

---

## 4.1 Потребителски роли

### 4.1.1 Модел на достъп

Контролът на достъпа в приложението се базира на **ролеви модел (RBAC)**. В изходния код ролите са дефинирани в изброяем тип `UserRole` и се записват в колона `role` на таблица `users`. При всяка заявка Spring Security зарежда потребителя по имейл и му присвоява съответното право (`GrantedAuthority`). Отделно от ролята се пази **статус на акаунта** — `ACTIVE` или `BLOCKED`; блокиран потребител не може да влезе в системата, което позволява на администратор да прекрати злоупотреба без изтриване на данни.

В системата са предвидени три роли:

- **`ROLE_PASSENGER`** (пътник) — роля по подразбиране при регистрация;
- **`ROLE_DRIVER`** (шофьор) — потребител, който създава и управлява пътувания;
- **`ROLE_ADMIN`** (администратор) — потребител с права за управление на платформата.

### 4.1.2 Пътник (`ROLE_PASSENGER`)

Пътникът е основният краен потребител на услугата. Той разглежда и търси пътувания, отваря детайли, резервира място и при нужда уточнява адрес и координати за качване. В базата данни резервацията се записва в таблица `bookings` със статус `PENDING` до момента, в който шофьорът я одобри или откаже. Пътникът използва чата и известията, за да координира действията си с шофьора, и след приключено пътуване може да остави оценка. Той **не** разполага с права за одобрение на чужи заявки и **няма** достъп до административните функции.

### 4.1.3 Шофьор (`ROLE_DRIVER`)

Шофьорът публикува пътувания чрез страницата за създаване на обява (`/rides/new`) и API заявка `POST /api/v1/rides`. За всяко негово пътуване той вижда постъпилите резервации и може да ги одобри или откаже. При одобрение намаляват се свободните места; когато местата се изчерпят, статусът на пътуването преминава в `FULL`. Шофьорът има достъп до обобщен списък с чакащи заявки и до изчисления **маршрут при тръгване**, който подрежда точките за качване и слизане по реда на обхождане. В кода е предвидена и защита срещу опит шофьорът да резервира собствено пътуване.

Важно е да се отбележи, че в настоящата реализация **няма отделен сървърен „начален екран“** за шофьор, както при някои по-стари системи. След вход всички потребители попадат на списъка с пътувания; допълнителните възможности за шофьор се проявяват в **навигационното меню** и в **страницата с детайли** на неговите обяви.

### 4.1.4 Администратор (`ROLE_ADMIN`)

Администраторът управлява регистрираните потребители — преглежда ги в таблица, блокира или отблокира акаунти и вижда обобщени показатели (брой потребители, брой пътувания). На сървъра достъпът е ограничен с `@PreAuthorize("hasRole('ADMIN')")` върху контролера `AdminController`; на клиента страницата `/admin` се показва само при роля `ROLE_ADMIN`.

### 4.1.5 Сравнение с йерархични роли в образец

В приложена като образец дипломна работа (градска информационна платформа) след вход един сървърен метод пренасочва потребителя към различна начална страница според ролята — регистриран потребител, модератор, администратор на град или главен администратор. Логиката е реализирана в рамките на **един контролер** и **сървърно рендирани HTML изгледи** (Thymeleaf).

В настоящия проект йерархията е **опростена до три роли**, което отговаря на домейна на carpool приложение. Разликата по роля се проявява в **динамичното меню на React**, в **съдържанието на отделните страници** и в **авторизацията на REST endpoints**, а не в пренасочване към различен сървърен шаблон. Този подход е по-подходящ за SPA архитектурата, избрана в глава III.

---

## 4.2 Програмна реализация

В следващите подраздели се описва реализацията по функционални области. За всяка област се дава обяснение на потребителското действие, на съответните API заявки и на ключовата бизнес логика, придружено от препоръка за **фигура** — екран от работещото приложение и/или фрагмент от изходния код.

### 4.2.1 Архитектура на реализацията и поток на данните

Реализацията на сървъра следва **трислойния модел**, описан при проектирането: **Controller** приема HTTP заявки и валидира входните данни; **Service** съдържа бизнес правилата и транзакционната логика; **Repository** осигурява достъп до базата чрез Spring Data JPA. Обектите от базата (`Entity`) се преобразуват към **DTO** преди изпращане към клиента, за да не се излага вътрешната структура и чувствителни полета (например хеш на паролата).

На страната на клиента **страниците** (`pages/`) координират потребителските сценарии, **компонентите** (`components/`) — преизползваеми елементи като картата (`RideMap`), а **API модулът** (`api/`) централизира HTTP комуникацията. Състоянието за автентикация се пази в **`AuthContext`**, а съобщенията за грешки и успех — в **`ToastContext`**.

Типичният поток при защитена операция може да се обобщи така: потребителят извършва действие в интерфейса; React изпраща заявка с JWT; филтърът `JwtAuthenticationFilter` валидира токена и установява контекст на сигурност; съответният контролер делегира към услуга; услугата записва или чете от MySQL; отговорът се връща като JSON и интерфейсът се актуализира. При липса или изтичане на токен сървърът връща HTTP 401, а клиентът извежда потребителя към страницата за вход.

Публично достъпни (без JWT) остават endpoints за автентикация (`/api/v1/auth/**`), проверка на здравето на сървъра, геокодиране, webhook за плащания и документацията Swagger. Всички останали изискват валиден токен.

**[Фиг. 4.1. Поток на обработка на заявка в реализираната система]** — блокова схема или препратка към архитектурната диаграма от глава III, допълнена с JWT филтъра.

---

### 4.2.2 Регистрация, вход и управление на потребителски акаунти

#### Регистрация

Страницата за регистрация (`/register`) съдържа форма с полета за имейл, парола, име и по избор телефон. При изпращане клиентът извиква `POST /api/v1/auth/register`. На сървъра методът `AuthService.register` проверява дали имейлът вече не е зает, хешира паролата с **BCrypt**, създава запис в таблица `users` с роля `ROLE_PASSENGER` и статус `ACTIVE`, след което генерира JWT и го връща заедно с данните за потребителя (`UserDto`). Клиентът записва токена в `localStorage` и пренасочва потребителя към списъка с пътувания.

Ролята на потребителя в този проект е **единствена стойност в колона** `role` на таблица `users`, а не връзка many-to-many към отделна таблица за роли, както при някои по-сложни системи. Това опростява модела и е достатъчно при три роли.

**[Фиг. 4.2. Екран за регистрация на потребител]**

**[Фиг. 4.3. Фрагмент от модела на данните — същност потребител (таблица users)]** — препратка към ER диаграмата от глава III.

**Фиг. 4.4. Реализация на регистрацията (метод register в AuthService)**

```java
@Transactional
public AuthResponse register(RegisterRequest request) {
    if (userRepository.findByEmail(request.email()).isPresent()) {
        throw new IllegalArgumentException("Email is already in use");
    }
    UserRole role = request.role() != null ? request.role() : UserRole.ROLE_PASSENGER;
    UserEntity user = UserEntity.builder()
            .email(request.email())
            .passwordHash(passwordEncoder.encode(request.password()))
            .name(request.name())
            .phone(request.phone())
            .role(role)
            .status(UserStatus.ACTIVE)
            .build();
    UserEntity saved = userRepository.save(user);
    UserDetails springUser = org.springframework.security.core.userdetails.User
            .withUsername(saved.getEmail())
            .password(saved.getPasswordHash())
            .authorities(saved.getRole().name())
            .build();
    String token = jwtService.generateToken(springUser);
    return new AuthResponse(token, UserMapper.toDto(saved));
}
```

#### Вход в системата

На страницата за вход (`/login`) потребителят подава имейл и парола. Заявката `POST /api/v1/auth/login` преминава през `AuthenticationManager` на Spring Security. При успешна проверка се генерира нов JWT. При грешни данни се връща съобщение за неуспешен вход (HTTP 401), което клиентът показва в разбираема форма.

Реализацията на входа в `AuthService.login` е следната:

```java
@Transactional(readOnly = true)
public AuthResponse login(LoginRequest request) {
    Authentication auth = authenticationManager.authenticate(
            new UsernamePasswordAuthenticationToken(
                    request.email(),
                    request.password()
            )
    );
    UserDetails principal = (UserDetails) auth.getPrincipal();
    UserEntity user = userRepository.findByEmail(principal.getUsername())
            .orElseThrow(() -> new IllegalArgumentException("User not found"));
    String token = jwtService.generateToken(principal);
    return new AuthResponse(token, UserMapper.toDto(user));
}
```

**[Фиг. 4.5. Екран за вход в системата]**

Класът `AuthController` обединява endpoints за регистрация, вход, смяна на парола и възстановяване при забравена парола.

**Фиг. 4.6. REST контролер за автентикация (AuthController, фрагмент)**

```java
@RestController
@RequestMapping("/api/v1/auth")
@RequiredArgsConstructor
public class AuthController {
    private final AuthService authService;

    @PostMapping("/register")
    public ResponseEntity<AuthResponse> register(@Valid @RequestBody RegisterRequest request) {
        return ResponseEntity.ok(authService.register(request));
    }

    @PostMapping("/login")
    public ResponseEntity<AuthResponse> login(@Valid @RequestBody LoginRequest request) {
        return ResponseEntity.ok(authService.login(request));
    }
}
```

---

### 4.2.3 Автентикация с JWT и защита на ресурсите

При SPA архитектурата сървърът не поддържа класическа сесия в памет. Вместо това след вход клиентът получава **JWT**, който съдържа идентификатор на потребителя (имейл) и информация за срока на валидност, криптографски подписан със секрет, конфигуриран в приложението. При всяка следваща заявка филтърът `JwtAuthenticationFilter` извлича токена от заглавка `Authorization`, проверява подписа и срока, зарежда потребителя чрез `CustomUserDetailsService` и установява контекст на автентикация в `SecurityContextHolder`.

Класът `CustomUserDetailsService` търси потребителя по имейл, предава хеша на паролата към Spring Security (за съвместимост с механизма за login) и му присвоява ролята като право. Ако акаунтът е блокиран, той се маркира като недостъпен (`disabled`).

**Фиг. 4.7. Зареждане на потребител и определяне на роля (CustomUserDetailsService)**

```java
@Override
public UserDetails loadUserByUsername(String email) throws UsernameNotFoundException {
    UserEntity user = userRepository.findByEmail(email)
            .orElseThrow(() -> new UsernameNotFoundException("User not found"));
    return User.withUsername(user.getEmail())
            .password(user.getPasswordHash())
            .authorities(List.of(new SimpleGrantedAuthority(user.getRole().name())))
            .disabled(user.getStatus() == UserStatus.BLOCKED)
            .build();
}
```

**Фиг. 4.8. Валидиране на JWT при входяща заявка (JwtAuthenticationFilter, фрагмент)**

```java
final String authHeader = request.getHeader("Authorization");
if (authHeader == null || !authHeader.startsWith("Bearer ")) {
    filterChain.doFilter(request, response);
    return;
}
final String jwt = authHeader.substring(7);
final String userEmail = jwtService.extractUsername(jwt);
if (userEmail != null && SecurityContextHolder.getContext().getAuthentication() == null) {
    UserDetails userDetails = userDetailsService.loadUserByUsername(userEmail);
    if (jwtService.isTokenValid(jwt, userDetails)) {
        UsernamePasswordAuthenticationToken authToken =
                new UsernamePasswordAuthenticationToken(
                        userDetails, null, userDetails.getAuthorities());
        SecurityContextHolder.getContext().setAuthentication(authToken);
    }
}
filterChain.doFilter(request, response);
```

Конфигурацията в `SecurityConfig` задава безсъстоянна сесия, правила за CORS (достъп от локален frontend и от устройства в локалната мрежа при тестване), изключване на CSRF за REST API с JWT, както и регистрация на филтрите за ограничаване на честотата на опити за вход и за JWT. Допълнително е предвиден филтър `AuthRateLimitFilter`, който намалява риска от brute force атаки върху login и register.

На клиента компонентът `ProtectedRoute` обвива страници, изискващи вход. При липса на валиден токен потребителят се пренасочва към `/login`. При стартиране на приложението `AuthProvider` опитва да възстанови сесията от `localStorage` чрез заявка `GET /api/v1/users/me`. Глобален обработчик в `App.tsx` при отговор 401 извършва изход и уведомява потребителя, че сесията е изтекла.

**Фиг. 4.9. Конфигурация на Spring Security (SecurityConfig, фрагмент)**

```java
@Bean
public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
    http
            .cors(cors -> cors.configurationSource(corsConfigurationSource()))
            .csrf(csrf -> csrf.disable())
            .sessionManagement(sm -> sm.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
            .authorizeHttpRequests(auth -> auth
                    .requestMatchers(
                            "/api/v1/auth/**",
                            "/api/v1/geocode",
                            "/api/v1/health",
                            "/api/v1/payments/webhook",
                            "/swagger-ui/**"
                    ).permitAll()
                    .anyRequest().authenticated())
            .addFilterBefore(authRateLimitFilter, UsernamePasswordAuthenticationFilter.class)
            .addFilterBefore(jwtAuthenticationFilter, UsernamePasswordAuthenticationFilter.class);
    return http.build();
}
```

**Фиг. 4.10. Защита на маршрути в клиентското приложение (ProtectedRoute)**

```tsx
function ProtectedRoute({ children }: { children: React.ReactNode }) {
  const { isAuthenticated, loading } = useAuth()
  const location = useLocation()
  if (loading) return <div className="app-main"><p>Зареждане...</p></div>
  if (!isAuthenticated) {
    return <Navigate to="/login" state={{ from: location }} replace />
  }
  return <>{children}</>
}
```

---

### 4.2.4 Потребителски интерфейс и навигация

Интерфейсът на приложението е изграден на български език, с ясна навигация в горната лента. След вход потребителят вижда връзки към основните раздели: пътувания, мои резервации, съобщения и профил. Ако потребителят има роля шофьор и активни пътувания с чакащи заявки, в менюто се появяват допълнителни връзки към „Маршрут при тръгване“ и „Чакащи резервации“, като последната може да показва броя на неразгледаните заявки. За администратор се визуализира връзка „Админ“.

Отделен елемент е **камбаната за известия**, която периодично зарежда данни от сървъра. При известие за нова резервация шофьорът може директно от падащото меню да одобри или откаже заявката, без да търси ръчно съответната резервация в списъка — това подобрява удобството при работа от мобилно устройство.

**[Фиг. 4.11. Начален екран — търсене и преглед на пътувания]**

**[Фиг. 4.12. Навигационна лента с известия]**

**[Фиг. 4.13. Административен панел]**

---

### 4.2.5 Търсене, създаване и преглед на пътувания

Списъкът с пътувания се зарежда чрез `GET /api/v1/rides`. При подадени критерии за търсене (градове, дата, страница, размер на страница, поле и посока на сортиране) сървърът връща странициран резултат (`Page<RideDto>`); в противен случай се връща пълният наличен списък. Така се реализират изискванията от глава II за филтриране и подреждане на обявите.

Създаването на пътуване става от страницата `/rides/new`. Потребителят въвежда градове и по желание квартали, дата и час на тръгване, цена, брой места и описание на автомобила. Координатите могат да се получат чрез геокодиране. При запис на сървъра се създава entity `RideEntity` със статус `OPEN`, може да се изчисли и запише полилиния на маршрута, както и начална и крайна спирка в таблица `ride_stops`.

**Фиг. 4.14. Филтриране на пътувания (RideController.getRides, фрагмент)**

```java
@GetMapping
public ResponseEntity<?> getRides(
        @RequestParam(required = false) String fromCity,
        @RequestParam(required = false) String toCity,
        @RequestParam(required = false) @DateTimeFormat(iso = DateTimeFormat.ISO.DATE) LocalDate date,
        @RequestParam(required = false) Integer page,
        @RequestParam(required = false) Integer size,
        @RequestParam(required = false) String sortBy,
        @RequestParam(required = false) String sortDir) {
    boolean useFilter = (fromCity != null && !fromCity.isBlank())
            || (toCity != null && !toCity.isBlank())
            || date != null
            || (page != null && page > 0)
            || (size != null && size > 0);
    if (useFilter) {
        int p = page != null ? page : 0;
        int s = size != null && size > 0 ? size : 20;
        return ResponseEntity.ok(rideService.getRidesFiltered(
                fromCity, toCity, date, p, s, sortBy, sortDir));
    }
    return ResponseEntity.ok(rideService.getAllRides());
}
```

**[Фиг. 4.15. Форма за създаване на ново пътуване]**

**Фиг. 4.16. Създаване на пътуване (RideController.createRide)**

```java
@PostMapping
public ResponseEntity<RideDto> createRide(
        @AuthenticationPrincipal UserDetails userDetails,
        @Valid @RequestBody RideCreateRequest request) {
    Long driverId = userRepository.findByEmail(userDetails.getUsername())
            .orElseThrow(() -> new IllegalArgumentException("User not found"))
            .getId();
    return ResponseEntity.ok(rideService.createRide(driverId, request));
}
```

Страницата с детайли (`/rides/:id`) е централна за потребителското изживяване. Тя комбинира текстова информация, карта с маршрут и спирки, панел за резервация (за пътник), списък с резервации и бутони за одобрение (за шофьор), вграден чат и възможност за оценка след приключване. Компонентът `RideMap` използва библиотека **Leaflet** и карти **OpenStreetMap**; маршрутът се визуализира като полилиния, а спирките — с цветни маркери (старт, край, качване, слизане).

**[Фиг. 4.17. Детайли на пътуване с карта и функции за резервация или управление]**

---

### 4.2.6 Резервации и одобрение от шофьор

Резервацията е ключовата транзакция в системата. При създаване се проверява дали шофьорът не се опитва да резервира собствено пътуване, дали няма вече активна резервация за същото пътуване и дали има достатъчно свободни места. Записът получава статус `PENDING`. Създава се разговор за чат и известие от тип `BOOKING_REQUEST` към шофьора. На фиг. 4.20а е показан опростен фрагмент от създаването на резервация в `BookingService`:

**Фиг. 4.20а. Създаване на резервация (BookingService.create, фрагмент)**

```java
@Transactional
public BookingDto create(Long rideId, Long passengerId) {
    RideEntity ride = rideRepository.findById(rideId)
            .orElseThrow(() -> new IllegalArgumentException("Ride not found"));
    if (ride.getDriver() != null && ride.getDriver().getId().equals(passengerId)) {
        throw new IllegalArgumentException("Driver cannot book own ride");
    }
    if (bookingRepository.existsByRide_IdAndPassenger_IdAndStatusIn(
            rideId, passengerId,
            List.of(BookingStatus.PENDING, BookingStatus.PENDING_PAYMENT, BookingStatus.APPROVED))) {
        throw new IllegalArgumentException("Already booked");
    }
    if (ride.getAvailableSeats() == null || ride.getAvailableSeats() <= 0) {
        throw new IllegalArgumentException("No available seats");
    }
    UserEntity passenger = userRepository.findById(passengerId)
            .orElseThrow(() -> new IllegalArgumentException("User not found"));
    BookingEntity entity = BookingEntity.builder()
            .ride(ride)
            .passenger(passenger)
            .status(BookingStatus.PENDING)
            .build();
    entity = bookingRepository.save(entity);
    conversationService.ensureConversationForBooking(
            ride.getId(), ride.getDriver().getId(), passenger.getId());
    notifyDriverForBookingRequest(ride, passenger, entity);
    return BookingMapper.toDto(entity);
}
```

Жизненият цикъл на резервацията може да протече по два основни сценария. При плащане **в брой** одобрението от шофьора директно превежда резервацията в статус `APPROVED` и намалява свободните места. При плащане с **карта** след одобрение резервацията преминава в `PENDING_PAYMENT`, пътникът получава известие `PAYMENT_REQUIRED` и трябва да завърши плащането през Stripe, преди участието да бъде окончателно потвърдено.

**[Фиг. 4.18. Преглед на собствените резервации]**

**[Фиг. 4.19. Чакащи резервации — изглед за шофьор]**

**Фиг. 4.20. Одобрение на резервация (BookingService.approve)**

```java
@Transactional
public BookingDto approve(Long bookingId, Long driverId) {
    BookingEntity entity = bookingRepository.findById(bookingId)
            .orElseThrow(() -> new IllegalArgumentException("Booking not found"));
    RideEntity ride = entity.getRide();
    if (ride.getDriver() == null || !ride.getDriver().getId().equals(driverId)) {
        throw new IllegalArgumentException("Not driver of this ride");
    }
    if (entity.getStatus() != BookingStatus.PENDING) {
        throw new IllegalArgumentException("Booking not pending");
    }
    if (entity.getPaymentMethod() == PaymentMethod.CARD) {
        entity.setStatus(BookingStatus.PENDING_PAYMENT);
        entity.setPaymentStatus(PaymentStatus.PENDING);
        notificationService.create(
                entity.getPassenger().getId(),
                NotificationType.PAYMENT_REQUIRED,
                "Резервацията е одобрена - необходимо е плащане",
                "Вашата резервация беше одобрена. Моля, платете с карта.",
                entity, ride);
    } else {
        entity.setStatus(BookingStatus.APPROVED);
        notificationService.create(
                entity.getPassenger().getId(),
                NotificationType.BOOKING_APPROVED,
                "Резервацията е одобрена",
                "Вашата резервация беше одобрена",
                entity, ride);
    }
    entity = bookingRepository.save(entity);
    int seats = entity.getSeatsReserved() != null ? entity.getSeatsReserved() : 1;
    ride.setAvailableSeats(ride.getAvailableSeats() - seats);
    if (ride.getAvailableSeats() <= 0) {
        ride.setStatus(RideStatus.FULL);
    }
    rideRepository.save(ride);
    return BookingMapper.toDto(entity);
}
```

Пътникът може допълнително да уточни място за качване чрез `PATCH /api/v1/bookings/{id}/pickup`. Системата поддържа валидация на избрани точки спрямо полилинията на маршрута, за да се гарантира, че качването е в разумна близост до пътя на пътуването.

---

### 4.2.7 Карта, маршрут и маршрут при тръгване

Географската визуализация е съществен елемент от приложението. За всеки преглед на пътуване се зарежда маршрутът и списъкът спирки чрез `GET /api/v1/rides/{id}/route`. За шофьора, който се подготвя да тръгне, е предвиден отделен endpoint `GET /api/v1/rides/{id}/driver-route`, достъпен само за собственика на обявата. Той изгражда последователност от точки: начало на пътуването, спирки, свързани с активни резервации (подредени по `stop_order`), и крайна точка. Върху тази последователност се изчислява полилиния за движение, която се показва на картата и помага на шофьора да планира реда на забиране на пътниците.

**[Фиг. 4.21. Маршрут при тръгване — визуализация на карта]**

**Фиг. 4.22. Изчисляване на маршрут за шофьор (RideService.getDriverRoute, фрагмент)**

```java
@Transactional(readOnly = true)
public RouteDto getDriverRoute(Long rideId, Long driverId) {
    RideDto ride = getRideById(rideId);
    if (!ride.getDriverId().equals(driverId)) {
        throw new AccessDeniedException("Only the driver can request driver route");
    }
    List<double[]> waypoints = new ArrayList<>();
    if (ride.getFromLat() != null && ride.getFromLng() != null) {
        waypoints.add(new double[]{ride.getFromLat(), ride.getFromLng()});
    }
    if (ride.getToLat() != null && ride.getToLng() != null) {
        waypoints.add(new double[]{ride.getToLat(), ride.getToLng()});
    }
    return routeService.getDrivingRouteWithWaypoints(waypoints);
}
```

---

### 4.2.8 Комуникация, известия и оценки

Чатът позволява директен обмен на съобщения между шофьор и пътник в контекста на конкретно пътуване. Съобщенията се пазят в таблица `chat_messages`; разговорите могат да се групират в `chat_conversations`. API предоставя изпращане и извличане на история за двойка участници. На страницата с детайли чатът е вграден; отделно съществува страница „Съобщения“ за преглед на всички активни разговори.

**[Фиг. 4.23. Екран за съобщения]**

**Фиг. 4.24. Изпращане на съобщение (ChatController)**

```java
@PostMapping
public ResponseEntity<ChatMessageDto> sendMessage(
        @AuthenticationPrincipal UserDetails userDetails,
        @Valid @RequestBody ChatMessageCreateRequest request) {
    Long senderId = userRepository.findByEmail(userDetails.getUsername())
            .orElseThrow(() -> new IllegalArgumentException("User not found"))
            .getId();
    return ResponseEntity.ok(chatService.sendMessage(
            request.rideId(), request.receiverId(), request.content(), senderId));
}
```

Известията допълват чата със структурирани събития — нова заявка, одобрение, отказ, изискване за плащане. Те се записват в таблица `notifications` и се показват в камбаната. Този механизъм намалява риска шофьорът да пропусне нова резервация.

След приключено пътуване (`FINISHED`) участниците могат да оставят оценка чрез `POST /api/v1/ratings`. Данните се съхраняват в таблица `ratings` и могат да актуализират средната оценка в профила на потребителя.

**[Фиг. 4.25. Профил на потребител с оценки]**

---

### 4.2.9 Плащания и възстановяване на парола

Плащането с карта се реализира чрез **Stripe Checkout**. След одобрение на резервация с избран метод CARD пътникът инициира плащане; сървърът създава сесия за плащане и пренасочва браузъра към защитена страница на Stripe. Потвърждението за успешно плащане се получава чрез webhook към `/api/v1/payments/webhook`, който актуализира статуса в базата данни. Така се изпълнява изискването от глава II за интеграция с външна платежна система, без приложението да съхранява данни за банкови карти.

Механизмът за забравена парола генерира еднократен токен с ограничен срок (30 минути), изпраща линк за нулиране по имейл или го отпечатва в конзолата на сървъра при тестови домейни, и позволява задаване на нова парола с изисквания за сложност.

**[Фиг. 4.26. Плащане с карта / потвърждение за успешно плащане]**

**[Фиг. 4.27. Възстановяване на забравена парола]**

---

### 4.2.10 Административен панел

Административният панел обслужва нуждите от поддръжка на платформата. Чрез него администраторът преглежда всички потребители, блокира или отблокира акаунти и вижда обобщена статистика. Достъпът е защитен както на ниво интерфейс, така и на ниво API.

**Фиг. 4.28. Административен контролер (AdminController)**

```java
@RestController
@RequestMapping("/api/v1/admin")
@PreAuthorize("hasRole('ADMIN')")
@RequiredArgsConstructor
public class AdminController {
    private final AdminService adminService;

    @GetMapping("/users")
    public ResponseEntity<List<UserDto>> getAllUsers() {
        return ResponseEntity.ok(adminService.getAllUsers());
    }

    @PostMapping("/users/{id}/block")
    public ResponseEntity<UserDto> blockUser(@PathVariable Long id) {
        return ResponseEntity.ok(adminService.blockUser(id));
    }

    @PostMapping("/users/{id}/unblock")
    public ResponseEntity<UserDto> unblockUser(@PathVariable Long id) {
        return ResponseEntity.ok(adminService.unblockUser(id));
    }

    @GetMapping("/stats")
    public ResponseEntity<AdminStatsDto> getStats() {
        return ResponseEntity.ok(adminService.getStats());
    }
}
```

---

### 4.2.11 Обработка на грешки и качество на потребителското изживяване

Приложението използва централизиран `GlobalExceptionHandler`, който преобразува изключенията в единен JSON формат (`ErrorResponse`) с полета за съобщение, код и при нужда детайли за валидационни грешки. На клиента тези съобщения се показват чрез система за тостове, а контейнерът за известия е маркиран с атрибути за достъпност (`role="alert"`, `aria-live="polite"`), което подобрява използваемостта от потребители с помощни технологии.

**Фиг. 4.29. Обработка на забранен достъп (GlobalExceptionHandler)**

```java
@ExceptionHandler(AccessDeniedException.class)
public ResponseEntity<ErrorResponse> handleAccessDenied(AccessDeniedException ex) {
    return ResponseEntity.status(HttpStatus.FORBIDDEN)
            .body(ErrorResponse.builder()
                    .message("Нямате права за това действие.")
                    .code("FORBIDDEN")
                    .build());
}
```

Така потребителят получава ясна обратна връзка при грешка, а не технически детайли, което е в съответствие с добрите практики при проектиране на потребителски интерфейс.

---

## Обобщение на глава IV

В настоящата глава е представена цялостната реализация на уеб приложението за споделени пътувания. Описани са потребителските роли и тяхното отражение в интерфейса и в API, механизмите за регистрация и JWT автентикация, основните екрани и функционални потоци — от търсене и резервация през карта и комуникация до плащане и администрация. Приведените фрагменти от изходния код илюстрират връзката между изискванията от глава II, проектирането от глава III и работещия софтуерен продукт. За пълна убедителност на изложението към текста трябва да бъдат добавени **фигури** — скрийншоти от работещото приложение и съответните фрагменти код, номерирани последователно след фигурите от глава III.

---

# ЗАКЛЮЧЕНИЕ

Съвременният начин на живот поставя все по-големи изисквания към мобилността и ефективното използване на транспортните ресурси. Споделените пътувания предлагат практична възможност за намаляване на разходите и на натоварването по пътищата, като същевременно запазват гъвкавостта на индивидуалното пътуване. Разработеното в рамките на настоящата дипломна работа уеб приложение адресира тази потребност, като обединява в една платформа процесите по обявяване, търсене, резервация, договаряне на детайли, комуникация и плащане.

Основната цел на проекта — да се реализира функционална и използваема система в съответствие с формулираните изисквания — **е постигната**. Потребителите могат сигурно да се регистрират и да влизат в приложението; да откриват подходящи пътувания; да резервират места и да виждат маршрута на карта; шофьорите да управляват заявките и да планират маршрута си при тръгване; страните да комуникират и да получават известия; администраторът да поддържа ред в платформата. Практическата стойност на решението се изразява в това, че разпръснатите днес неформални канали (социални мрежи, телефонни обаждания) се заменят с **проследима и структурирана среда**.

Избраният технологичен стек — **React**, **Spring Boot** и **MySQL** — се оказва подходящ за изграждането на подобно приложение: позволява ясно разделение на отговорностите, добра поддръжка на сигурност и REST комуникация, както и възможност за по-нататъшно разширяване. В перспектива могат да се развият функции за комуникация в реално време, мобилно приложение, по-пълна GPS навигация и разширени административни анализи. Подготвената контейнеризация с Docker улеснява разгръщането в production среда. Независимо от тези насоки за развитие, текущата реализация представлява **завършен и демонстрируем прототип**, който потвърждава приложимостта на направените проектни решения и обосновава темата на дипломната работа.

---

## Списък на фигурите (глава IV)

| Фигура | Препоръчително съдържание |
|--------|-------------------------|
| 4.1 | Схема на потока на заявка |
| 4.2 | Екран „Регистрация“ |
| 4.3 | ER / таблица users |
| 4.4 | Код: AuthService.register |
| 4.5 | Екран „Вход“ |
| 4.6 | Код: AuthController |
| 4.7 | Код: CustomUserDetailsService |
| 4.8 | Код: JwtAuthenticationFilter |
| 4.9 | Код: SecurityConfig |
| 4.10 | Код: ProtectedRoute |
| 4.11 | Екран „Пътувания“ |
| 4.12 | Камбана с известия |
| 4.13 | Админ панел |
| 4.14 | Код: getRides |
| 4.15 | Екран „Ново пътуване“ |
| 4.16 | Код: createRide |
| 4.17 | Детайли + карта |
| 4.18 | Мои резервации |
| 4.19 | Чакащи резервации |
| 4.20 | Код: approve |
| 4.21 | Маршрут при тръгване |
| 4.22 | Код: getDriverRoute |
| 4.23 | Съобщения |
| 4.24 | Код: ChatController |
| 4.25 | Профил |
| 4.26 | Плащане |
| 4.27 | Забравена парола |
| 4.28 | Код: AdminController |
| 4.29 | Код: GlobalExceptionHandler |

*Тестов акаунт: test@example.com / password123*
