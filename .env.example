# Backend (optional – defaults work for local dev)
# SPRING_DATASOURCE_URL=jdbc:mysql://localhost:3306/carpool_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# SPRING_DATASOURCE_USERNAME=root
# SPRING_DATASOURCE_PASSWORD=root
# JWT_SECRET=<base64-encoded-secret-at-least-256-bits>
# JWT_EXPIRY_HOURS=24

# Docker Compose overrides
# JWT_SECRET=your-production-secret-base64
# JWT_EXPIRY_HOURS=24
