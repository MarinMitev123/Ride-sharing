{{- if .Values.frontend.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "carpool.frontend.fullname" . }}
  labels:
    {{- include "carpool.frontend.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.frontend.port }}
      targetPort: http
  selector:
    {{- include "carpool.frontend.selectorLabels" . | nindent 4 }}
{{- end }}
